my_new_function = lambda abcd: abcd*2

print(my_new_function(10))

# A lambda funtion is a small, annonimous function written in one single line
# lambda arguments: Expression
# lambda can return only one expression
# list comprehension must be inside brackets []
